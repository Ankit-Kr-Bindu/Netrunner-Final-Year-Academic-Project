- name: Show Access Lists
  hosts: "{{ target }}"
  gather_facts: no
  tasks:
    - name: Show Access Lists
      ios_command:
        commands:
          - show access-lists
      register: acl_info
